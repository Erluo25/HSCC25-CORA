%support_exp(10, 1);
%support_exp(20, 1);

%support_exp(25, 1);

%support_exp(30, 5); % Manually stop if more than 30 mins
%support_exp(30, 6);

%vardubin_exp("default"); % Get the 60 split figure

vardubin_exp("compare");